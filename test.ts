const states = {
    /**
     * test 函数 测试
     */
    test: 'ddf'
}

// const [name, setName] = useState(() => states.test)
// const [name, setName] = useState(states.test)

// setName('')
// setName(name => name + '');
